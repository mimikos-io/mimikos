openapi: "3.0.3"
info:
  title: E2E Stateful Test API
  version: "1.0.0"
  description: >
    Purpose-built spec for stateful mode patterns not covered by Petstore or Asana.
    Tests: flat single-resource + object-wrapped list (Stripe pattern),
    non-"data" array key, ID fallback via body "id", pagination metadata.
paths:
  # --- Pattern 1: Flat single-resource + object-wrapped list ---
  # Stripe pattern: GET /customers/{customer} returns flat {id, name, email}
  # but GET /customers returns {results: [...], has_more: bool, total: int}.
  /customers:
    get:
      operationId: listCustomers
      responses:
        "200":
          description: Paginated list of customers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerList"
    post:
      operationId: createCustomer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewCustomer"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
  /customers/{customer}:
    get:
      operationId: getCustomer
      parameters:
        - name: customer
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Customer details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
    put:
      operationId: updateCustomer
      parameters:
        - name: customer
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewCustomer"
      responses:
        "200":
          description: Updated customer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
    delete:
      operationId: deleteCustomer
      parameters:
        - name: customer
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  # --- Pattern 2: Nested resource with parent scope isolation ---
  # Tests that /organizations/org1/teams and /organizations/org2/teams
  # have separate state (parent-scoped resource isolation).
  /organizations/{orgId}/teams:
    get:
      operationId: listOrgTeams
      parameters:
        - name: orgId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: List of teams in the organization
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Team"
    post:
      operationId: createOrgTeam
      parameters:
        - name: orgId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewTeam"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Team"
  /organizations/{orgId}/teams/{teamId}:
    get:
      operationId: getOrgTeam
      parameters:
        - name: orgId
          in: path
          required: true
          schema:
            type: string
        - name: teamId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Team details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Team"
    put:
      operationId: updateOrgTeam
      parameters:
        - name: orgId
          in: path
          required: true
          schema:
            type: string
        - name: teamId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewTeam"
      responses:
        "200":
          description: Updated team
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Team"
    delete:
      operationId: deleteOrgTeam
      parameters:
        - name: orgId
          in: path
          required: true
          schema:
            type: string
        - name: teamId
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  # --- Pattern 3: Object-wrapped list with non-"data" array key ---
  /products:
    get:
      operationId: listProducts
      responses:
        "200":
          description: Paginated list of products
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductList"
    post:
      operationId: createProduct
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewProduct"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
  /products/{productId}:
    get:
      operationId: getProduct
      parameters:
        - name: productId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Product details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"

components:
  schemas:
    Team:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string

    NewTeam:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
        description:
          type: string

    Customer:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string
        email:
          type: string
          format: email

    NewCustomer:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
        email:
          type: string

    CustomerList:
      type: object
      required:
        - results
        - has_more
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/Customer"
        has_more:
          type: boolean
        total:
          type: integer

    Product:
      type: object
      required:
        - id
        - title
      properties:
        id:
          type: string
        title:
          type: string
        price:
          type: number

    NewProduct:
      type: object
      required:
        - title
      properties:
        title:
          type: string
          minLength: 1
        price:
          type: number

    ProductList:
      type: object
      required:
        - items
        - page
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Product"
        page:
          type: integer
        total_pages:
          type: integer
